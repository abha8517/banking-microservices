spring:
  application:
    name: account-service
  datasource:
    url: jdbc:postgresql://postgres:5432/banking
    username: postgres
    password: postgres
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
